import type Report from '../../models/interfaces/report.js'
import type BaseRepository from './base-repository'

interface ReportRepository extends BaseRepository<Report> {

}

export default ReportRepository
